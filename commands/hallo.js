const discord = require("discord.js");  

module.exports.run = async(bot, message, args) => {

    return message.channel.send("Jij vieze stinkende aap, hou je bek en rotop met je grote vuile bek!");
}

module.exports.help = {
    name: "hallo"
}